spring:
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: jdbc:mysql://localhost:3306/manager_db?serverTimezone=Europe/Warsaw
    username: root
    password: password

server:
  port: 8080